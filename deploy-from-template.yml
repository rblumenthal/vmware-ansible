- name: Create a virtual machine from a template
  vmware_guest:
    hostname: "{{ uhmc-vcenter02 }}"
    username: "{{ svc_ansible@uhmc.sbuh.stonybrook.edu }}"
    password: "{{ eOjHMTXU1szBqDiANLwv }}"
    validate_certs: no
    folder: /Automation (Russ)
    name: russ-test-deploy01
    state: poweredon
    template: russ2016template
    disk:
    - size_gb: 40
      type: thin
      datastore: uhmc_vol_nfs_dev04 
    hardware:
      memory_mb: 4096
      num_cpus: 2
      num_cpu_cores_per_socket: 1
      scsi: paravirtual
      hotadd_cpu: True
      hotremove_cpu: True
      hotadd_memory: True
      version: 12 # Hardware version of virtual machine
      boot_firmware: "efi"
    networks:
    - name: dvVM_Sysadmin
    wait_for_ip_address: no
  delegate_to: localhost
  register: deploy